[mysqld]
datadir         = "./mysqld"
port            = 3305
pid-file        = "./mysqld.pid"
socket          = "./mysqld.sock"
mysqlx          = 0
server-id       = 1
gtid_mode       = ON
enforce_gtid_consistency = ON
default_authentication_plugin = mysql_native_password